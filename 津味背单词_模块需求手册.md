# 网页版「津味背单词」· 模块需求手册（可复现规格说明书）

> **用途**：本手册固化了从初版到最终版，用户对「网页版津味背单词」每一个模块提出过的全部需求。
> 将本文件整段作为 Prompt 输入 WorkBuddy（或同类 AI 编程助手），即可重新生成功能一致的单文件 HTML 网页程序。
> **程序形态**：单个 `.html` 文件（内嵌全部 JS/CSS/数据），双击即用，无需服务器。
> **配套资源**：`avatar.png`（登录卡通头像，需与 HTML 同目录）。

---

## 〇、项目总纲（必读）

| 项目 | 要求 |
|------|------|
| 程序名称 | **网页版津味背单词**（`<title>` 与页面主标题均为「津味背单词」） |
| 数据源 | 本地文本 `K:\2026年上海初中考纲词汇用法手册.txt`（2026 上海初中英语考纲词汇，约 2333 词） |
| 交付形态 | 单文件 HTML（数据内联注入，不依赖网络） |
| 视觉风格 | 马卡龙色系、玻璃拟态（glassmorphism）、可爱卡通兔子吉祥物（SVG，含鼓掌/星星/彩带动画） |
| 持久化 | 浏览器 `localStorage`（用户、学习历史、成就、错题本、自定义词库均本地存储） |
| 账号体系 | 纯本地模拟（无真实后端；找回密码仅前端模拟，UI 需注明"本机模拟，无真实邮件/短信"） |
| 音标标准 | 美式音标采用标准 IPA（取自 ipa-dict `en_US` 数据集），英式音标采用 `en_UK` 数据集；多词短语保留原书音标 |
| 发音 TTS | 使用浏览器 `speechSynthesis`（朗读英文单词），需预加载 en-US 语音、`suspended` 时 `resume()` |
| 关键工程经验 | **大文件（脚本 >500KB）禁用内联 `onclick`**，一律改用 `document.addEventListener` 事件委托，否则部分浏览器报 `xxx is not a function` |

---

## 一、原始 6 大基础模块（从首版起保留，不可删减）

1. **学习模式（Study）**：展示单词、音标（英+美）、词性、核心释义、真题例句。
2. **英翻中（E→C）**：给出英文单词，用户选出/输入正确中文释义。
3. **中翻英（C→E）**：给出中文释义，用户选出/输入正确英文单词。
4. **词性变化（Word Form）**：给出一个词的基础形，要求写出其常见派生词（名词/动词/形容词/副词等）。**该模块的输入框尺寸作为"大输入框"的参考基准**。
5. **统计（Stats）**：展示累计学习量、正确率、各模块进度等。
6. **错题本（Wrong Book）**：汇总所有答错的词，可点击复习、朗读、移除。

---

## 二、扩展模块需求明细（Round B 大扩展，7 个新模块）

### 模块 1 · 账号中心（登录 / 注册 / 找回密码）

**原始需求（用户原文要点）**
- 注册用户名 **5~18 位字符**，可由字母、数字或特殊符号任意组合。
- 注册须**关联电子邮箱**与**手机号码**。
- 系统自动检测重名：**用户名、电子邮箱、手机号码三者任一已存在即禁止注册**（三者均须唯一）。
- 密码 **5~18 位字符**，可由数字或特殊符号任意组合。
- 系统记录上述全部信息。
- 支持通过**电子邮箱**或**手机号码**找回 / 重置密码。

**后续修订需求**
- 注册成功后：**显示注册成功界面**（含账号名、🎉 图标），随后**自动跳转到登录页**（约 2 秒，也提供「立即登录 →」手动按钮）。
- 登录界面使用用户提供的卡通图片 `趣味记单词卡通形象设计 (5).png` 作为**背景/横幅**。
- 账号中心卡通图在模态框内**完整显示**（不裁剪，`object-fit:contain`），可按比例缩小（`max-height` 约 240px）。
- **登录后**，头像缩略图显示在**页面页眉（蓝色背景）的最右上角**，每个模块页都可见；点击头像可回到账号中心。

---

### 模块 2 · 拼写训练（Spelling）

**原始需求**
- 页面**只显示中文释义和例句**，**不显示英文单词**。
- 用户在输入框输入英文单词，系统判断是否与正确答案**完全一致**。
- **仅大小写不同算正确**（大小写不敏感）。
- 拼写错误时：显示"我的答案"与"正确答案"，并**用红色标出不同的字母**（逐字母 diff 高亮）。
- 答错的词**自动进入错题本**。

**后续修订需求**
- 输入框**尽可能大**（高度约 62px、字号约 22px、加粗），尺寸参考"词性变化"模块的大输入框并更大。
- 支持回车提交。

---

### 模块 3 · 今日训练计划（Plan）

**原始需求**
- 可设置今日训练单词数量（如 30 / 50 / 80）。
- 系统从**全部单词中随机抽取且不重复**。
- 页面显示进度（如 `12/50`）。
- 训练结束生成**总结**：正确数、错误数、错词列表。
- 总结可**一键复制为文本**（方便粘贴到备忘录）。

---

### 模块 4 · 标准单词背诵词卡（Flashcard）

**原始需求 · 正面**
1. 核心单词
2. 音标：**英音 + 美音**
3. 词性（**全部常考词性罗列**）

**原始需求 · 背面**
1. 核心释义（按考试频率排序；**不常考释义加粗标注**）
2. **2 个中考真题高频搭配**
3. **1 句中考古题同源例句**（附中文翻译）
4. **速记巧记方法**（助记口诀）
5. **通关打卡**模块（打卡记录）

**规则**：内容须**严格对齐 2026 上海考纲**，拒绝超纲内容。

---

### 模块 5 · 闯关打卡 / PK 对战 / 词库学习（Quest + PK + Library）

**视觉**：马卡龙配色、展览级玻璃拟态、SVG 兔子吉祥物（鼓掌动画 + 星星 + 彩带）。

**闯关打卡（Quest）**
- 5 个关卡、50 个单词的"世界杯阶梯"结构（预选赛 → 世界杯 → 奖杯）。
- 每关 **5 个填空挑战**：提供字母池、💡 提示按钮、清除按钮；通关评 **3 星**解锁。
- 成就 / 连击 / 经验值（XP）持久化（`localStorage`）。

**PK 对战（PK Battle）**
- 3 档 AI 对手：**菜鸟 / 学霸 / 战神**。
- 每场 **10 题**，**15 秒倒计时**抢答。
- 答对得分，答错/超时记录。

**词库学习（Library）**
- 50 张词卡浏览，支持 ✅ 标记。
- 成就 / 连击 / XP 展示。

**错题入库要求（后续补充）**：闯关打卡答错、PK 答错（含超时）均**计入错题本**。

---

### 模块 6 · 文档导入词库（Import）

**视觉**：阿根廷足球队队服配色主题入口卡片。

**功能**
- 模态框支持上传 `.txt` / `.csv`，或**手动输入**（字段：word, meaning, phonetic, example）。
- 实时预览导入内容。
- 保存为**自定义关卡**，可**删除 / 可玩 / 可浏览**。
- 难度选择：**25% / 35% / 50% / 65%** 留空比例（填空挖空率）。
- 游戏形式为 4 选 1 闯关。

**错题入库要求（后续补充）**：导入词库游戏中答错也**计入错题本**（自定义词若缺中文释义，用单词本身作提示）。

---

### 模块 7 · 闪卡 PK（Flashcard PK）

**原始需求**
- 基于词库全部单词，生成**英语单词 4 选 1** 的 PK 式闪卡。
- 提示音**随机中文或英文朗读**。
- 抽取范围可按**章节**或**整本**。
- 每轮 **10 个词**。
- 做对给予奖励（动画/积分）。
- **错词记忆**：本轮错词下一轮复现。
- 干扰项尽量**接近提示音对应的词**（相似音优先）。

**错题入库要求（后续补充）**：闪卡 PK 答错**计入错题本**。

---

## 三、统一错题本规则（后续修订，覆盖全部模块）

**要求**：所有模块学习过程中出现的错误，**统一统计进错题本**。最终覆盖模块清单：
- 英翻中、中翻英、词性变化、拼写训练、今日计划（原始 5 个）
- **新增**：闯关打卡、PK 对战（含超时）、闪卡 PK、导入词库游戏

> 实现要点：`addWrong()` 在判定为错误时立即调用，**置于渲染更新之前**，防止渲染异常阻断入库；对象字段与错题本渲染兼容（含 `mode`/`word`/`q`/`a`/`ans`/`toks`/`pos`）。

---

## 四、音标与发音（Round A 修复）

1. **美式音标必须标准 IPA**：原书为教材式注音（如 `ability /a'blati/`），须替换为标准美式 IPA（如 `/əˈbɪləti/`），数据源为 ipa-dict `en_US.txt`（约 12.6 万条）；英式音标用 `en_UK.txt`。覆盖率目标 ≥98%；英式拼写自动回退映射（ise→ize 等）；多词短语保留原书音标。
2. **点击单词必须发声**：修复内联 `onclick` 导致无声的问题（改事件委托 + `data-w` 属性）；`speechSynthesis` 预加载 en-US 语音， suspended 时 resume。

---

## 五、视觉与头像（历次修订汇总）

| 项 | 要求 |
|----|------|
| 主标题 | 页面 `<h1>` 与 `<title>` 均为「津味背单词」 |
| 登录背景图 | `趣味记单词卡通形象设计 (5).png` 作为账号中心横幅，`object-fit:contain` 完整显示、按比例缩小 |
| 登录后头像 | 注入页眉 `<header>` 最右上角（蓝色背景上方），圆形缩略图，点击回账号中心 |
| 吉祥物 | SVG 卡通兔子，含鼓掌 / 星星 / 彩带动画（用于通关、答对鼓励） |
| 配色 | 马卡龙色 + 紫色主调（#5a4bd1 / #7c5cff 等） |

---

## 六、复现指引（给 AI 的执行清单）

若要用本手册重新生成程序，请按以下顺序实施：

1. **解析词库**：读取 `K:\2026年上海初中考纲词汇用法手册.txt`，清洗 OCR 拼写错误，合并被误拆的词（如 `a few`、`captain`），校正美式/英式音标。
2. **构建数据**：生成 `DATA` 数组（每词含 `w`/`ph`(美)/`ph_uk`(英)/`pos`/`toks`(释义)/`ex`(例句) 等字段）。
3. **单文件骨架**：`build.py` 将 `DATA = __VOCAB_DATA__` 占位符注入 `template.html`，产出最终 `.html`。
4. **实现模块**：按上文"一~五"逐模块编码（基础 6 + 扩展 7）。
5. **关键防坑**：
   - 所有按钮/链接**禁用内联 `onclick`**，统一 `document.addEventListener('click', ...)` 事件委托（否则大文件报 `is not a function`）。
   - `save()/load()` 包 `try-catch`，防止 `localStorage` 受限中断流程。
   - 注册按钮加 `type="button"`，防表单误提交。
6. **测试**：用 `jsdom`（`runScripts:'dangerously'`）真实加载 HTML，模拟注册→跳转、各模块答错→错题本入库、头像注入页眉等，确认零报错。
7. **配套资源**：将 `avatar.png` 与最终 HTML 置于同目录。

---

## 七、需求演进时间线（便于追溯"如何形成最终版"）

| 阶段 | 关键动作 |
|------|----------|
| 首版 | 单文件 HTML 训练器，基础 6 模块，教材式音标，TTS 无声 |
| Round A | 校正美式标准 IPA；修复点击发音 |
| Round B | 大扩展 7 模块（账号/拼写/计划/词卡/闯关PK/导入/闪卡PK），保留原 6 模块 |
| 修订① | 注册成功页+跳转登录；改名"津味背单词"；登录背景图；登录后各模块右上角头像 |
| 调试 | 修复 `authSubmit is not a function`（事件委托）；修复旧缓存"假死" |
| 修订② | 账号图完整显示；头像移至页眉最右上角；所有模块错误入错题本；拼写输入框加大 |

---

*本手册由对话历史固化而成，可作为「网页版津味背单词」的完整需求基线。任何重新生成均应以此为准。*
